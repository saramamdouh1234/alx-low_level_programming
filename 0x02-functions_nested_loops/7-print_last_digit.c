#include "main.h"

/**
* print_last_digit - prints the last digit of a number
* @n: The numder to be treated
* Return: Value of the last digit of number
*/
int print_last_digit(int n)
{
int last;
last = n % 10;
if (las < 0)
{
last = last * -1;
}
_putchar(lastDigit + '0');
return (last);
}
